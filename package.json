{
  "name": "colonist-game-analyst",
  "version": "0.5.39",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test",
    "pack-extension": "cd extension && zip -r ../colonist-game-analyst-extension.zip . -x '*.DS_Store' -x 'icons/image.png' -x 'icons/icon.png'",
    "regenerate-icons": "cd extension/icons && sips -z 128 128 image.png --out icon-128.png && sips -z 48 48 image.png --out icon-48.png && sips -z 16 16 image.png --out icon-16.png"
  }
}
